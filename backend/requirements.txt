# TickerBoo backend dependencies

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client (async)
httpx>=0.27.0

# Data
numpy>=1.26.0

# Database
aiosqlite>=0.20.0

# Technical indicators (pre-built wheels available on Linux/macOS)
TA-Lib>=0.6.0

# Scheduling (async pre-compute jobs)
apscheduler>=3.10.0

# Config / env
python-dotenv>=1.0.0

# File uploads (FastAPI UploadFile)
python-multipart>=0.0.9

# Compression / parsing
lxml>=5.2.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx  # already listed above, pytest client uses it too
